#include <gtest/gtest.h>

#include <rdge/math/vec2.hpp>

using namespace rdge::math;

TEST(Vector2Test, HandlesConstruction)
{
    //EXPECT_TRUE(fp_eq(qnan32, qnan32));
    //EXPECT_TRUE(fp_eq(INFINITY, -INFINITY));
}
